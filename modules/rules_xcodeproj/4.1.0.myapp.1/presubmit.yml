bcr_test_module:
  module_path: "."
  matrix:
    platform: ["macos"]
    bazel: ["9.2.0"]
  tasks:
    verify_targets:
      name: "Verify build targets"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      build_targets:
      - "@rules_xcodeproj//xcodeproj/..."
